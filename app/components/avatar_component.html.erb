<% if @user.image? %>
  <%= image_tag @user.image, size: size, alt: @alt, class: "rounded-circle d-inline-block align-top" %>
<% else %>
  <i class="fa fa-user"></i>
<% end %>
<b><%= @user.username %></b>
