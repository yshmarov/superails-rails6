<%= form_with(model: [@commentable, comment]) do |form| %>
  <%= render 'layouts/errors', form: form %>

  <div class="field" data-controller="character-counter">
    <%= form.text_area :body,
      style: "width: 100%",
      rows: 2,
      data: { controller: "textarea-autogrow", character_counter_target: 'input' },
      maxlength: Comment::MAX_BODY_LENGTH, 
      placeholder: 'Add a comment here. User Markdown for formatting' 
    %>
    <strong data-character-counter-target="counter"></strong>/<%= Comment::MAX_BODY_LENGTH%> characters
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>