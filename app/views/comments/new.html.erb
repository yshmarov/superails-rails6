New comment for
<% if @commentable.model_name == 'Comment' %>
  <%= link_to @commentable.commentable.title, @commentable.commentable %>
<% else %>
  <%= link_to @commentable.title, @commentable %>
<% end %>
<%= render 'form', comment: @comment %>
