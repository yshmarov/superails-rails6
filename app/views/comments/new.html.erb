New comment for

<%= link_to @commentable.title, @commentable unless @commentable.is_a?(Comment) %>
<%= link_to @commentable.find_parent.title, @commentable.find_parent if @commentable.is_a?(Comment) %>
<%= render 'comments/form', comment: @comment %>
